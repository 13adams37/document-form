import { Notify } from 'quasar';

Notify.setDefaults({
  color: 'none',
  classes: 'q-notify__center',
  // position: 'top-right',
  // timeout: 2500,
  // textColor: 'white',
  // actions: [{ icon: 'close', color: 'white' }],
});

{
  /* <style>
.q-notification__message {
  text-align: center;
  white-space: break-spaces;
}
</style> */
}
